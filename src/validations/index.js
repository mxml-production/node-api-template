const AuthValidation = require('./auth.validation');
const UserValidation = require('./user.validation');
const EstablishmentValidation = require('./establishment.validation');

module.exports = {
    AuthValidation,
    UserValidation,
    EstablishmentValidation
}